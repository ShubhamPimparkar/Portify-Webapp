/**
 * Centralized API configuration
 * 
 * The API base URL can be set via the VITE_API_BASE_URL environment variable.
 * If not set, it defaults to the production Railway host.
 */
export const API_BASE_URL = import.meta.env.VITE_API_BASE_URL

